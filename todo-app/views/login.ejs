<html>
  <head>
    <title>TO-DO Manager</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="./css/style.css" rel="stylesheet">
        <title>Sanket Todo Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2 py-10">
        <h1 class="text-xl font-semibold mb-4 text-rose-600">To-Do Manager</h1>
        <h6 class="py-4">Login to you account</h6>
         <form action="/session" method="post">
         <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="py-3">
            <label for="email">Email: </label>
            <input name="email" type="email" class="border border-gray-300 text-gray-900 text-sm rounded w-full p-2" >
          </div>
          <div class="py-3">
            <label for="password">Password: </label>
            <input name="password" type="password" class="border border-gray-300 text-gray-900 text-sm rounded w-full p-2" >
          </div>
          <div class="py-3">
            <button type="submit" class="text-white bg-rose-600 hover:bg-rose-900 font-medium rounded text-sm px-5 py-2 mr-2 mb-2">login</button>
          </div>
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
            <% for(var i=0; i<messages.error.length; i++) { %>
              <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
            <% } %>
          <% } %>
      </div>
    </div>
  </body>
</html>